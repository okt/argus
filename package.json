{
  "name": "argus",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "^1.1.6"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun --hot run index.ts",
    "build": "bun build client.ts --outdir dist --target=bun",
    "clean": "rm -r dist/vlient.js; rm stats.db",
    "client": "bun dist/client.js"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "zod": "^3.23.8"
  }
}